linter:
  code_sniffer:
    version: 3
    dir: ./
    options:
      extensions: php,inc
      standard: PSR2
      encoding: utf-8

  phpmd:
    options:
      suffixes: php,inc
      exclude: vendor/
      strict: true

  eslint:
    npm_install: true
