linter:
  code_sniffer:
    version: 3
    dir: ./
    options:
      extensions: php,inc
      standard: PSR2
      encoding: utf-8
  phpmd:
    options:
      rule: codesize,naming,unusedcode
      minimumpriority: 3
      suffixes: php,inc
      exclude: vendor/,Vendor/
      strict: false
