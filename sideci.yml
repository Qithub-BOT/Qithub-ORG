linter:
  code_sniffer:
    dir: includes/
    dir: tools/
    dir: tests/
    options:
      extensions: php,inc
      standard: PSR2
      encoding: utf-8
  phpmd:
    options:
      suffixes: php,inc
      exclude: vendor/
      strict: true
